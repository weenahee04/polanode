# คู่มือการใช้งาน Maintenance Mode

## 📋 หน้าแจ้งเตือนปิดปรับปรุงเว็บไซต์

หน้า Maintenance Screen ถูกสร้างไว้แล้วและพร้อมใช้งาน

---

## 🚀 วิธีเปิด/ปิด Maintenance Mode

### วิธีที่ 1: ใช้ Environment Variable (แนะนำ)

#### สำหรับ Local Development

แก้ไข `.env.local`:
```env
# เปิด Maintenance Mode
VITE_MAINTENANCE_MODE=true

# ปิด Maintenance Mode (หรือลบบรรทัดนี้ออก)
# VITE_MAINTENANCE_MODE=false
```

**รีสตาร์ท dev server:**
```bash
npm run dev
```

#### สำหรับ Netlify (Production)

1. ไปที่ **Netlify Dashboard**
2. เลือก site ของคุณ
3. ไปที่ **Site settings** → **Environment variables**
4. เพิ่ม variable:
   - **Key:** `VITE_MAINTENANCE_MODE`
   - **Value:** `true` (เปิด) หรือ `false` (ปิด)
5. **Redeploy:**
   - ไปที่ Deploys tab
   - คลิก "Trigger deploy" → "Clear cache and deploy site"

---

### วิธีที่ 2: แก้ไขโค้ดโดยตรง

แก้ไข `App.tsx`:

```typescript
// เปิด Maintenance Mode
const MAINTENANCE_MODE = true;

// ปิด Maintenance Mode
const MAINTENANCE_MODE = false;
```

---

## 🎨 Customize หน้า Maintenance

แก้ไข `components/MaintenanceScreen.tsx` หรือส่ง props:

```typescript
<MaintenanceScreen
  title="เว็บไซต์ปิดปรับปรุงชั่วคราว"
  message="เรากำลังปรับปรุงระบบเพื่อให้บริการที่ดีขึ้นแก่คุณ"
  estimatedTime="ประมาณ 1-2 ชั่วโมง"
  contactEmail="support@thailife-rewards.com"
/>
```

---

## 📝 Props ที่สามารถ Customize ได้

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `title` | string | "เว็บไซต์ปิดปรับปรุงชั่วคราว" | หัวข้อหลัก |
| `message` | string | "เรากำลังปรับปรุงระบบ..." | ข้อความอธิบาย |
| `estimatedTime` | string | "ประมาณ 1-2 ชั่วโมง" | เวลาที่คาดการณ์ |
| `contactEmail` | string | "support@thailife-rewards.com" | อีเมลติดต่อ |

---

## 🎯 Use Cases

### 1. ปิดปรับปรุงระบบ
```env
VITE_MAINTENANCE_MODE=true
```

### 2. อัพเดทฐานข้อมูล
```env
VITE_MAINTENANCE_MODE=true
```

### 3. Deploy เวอร์ชันใหม่
```env
VITE_MAINTENANCE_MODE=true
```

### 4. แก้ไขปัญหาเร่งด่วน
```env
VITE_MAINTENANCE_MODE=true
```

---

## 🔄 Workflow แนะนำ

### ก่อนเริ่ม Maintenance

1. **เปิด Maintenance Mode**
   ```env
   VITE_MAINTENANCE_MODE=true
   ```

2. **Deploy/Redeploy**
   - ใน Netlify: ตั้งค่า environment variable แล้ว redeploy
   - ใน Local: รีสตาร์ท dev server

3. **ตรวจสอบ**
   - เปิดเว็บไซต์ ควรเห็นหน้า maintenance

### หลังเสร็จสิ้น Maintenance

1. **ปิด Maintenance Mode**
   ```env
   VITE_MAINTENANCE_MODE=false
   ```
   หรือลบ environment variable ออก

2. **Deploy/Redeploy**
   - ใน Netlify: เปลี่ยน environment variable แล้ว redeploy
   - ใน Local: รีสตาร์ท dev server

3. **ตรวจสอบ**
   - เปิดเว็บไซต์ ควรเห็นหน้าแอปปกติ

---

## 🎨 Design Features

หน้า Maintenance Screen มีฟีเจอร์:

- ✅ **Responsive Design** - รองรับทุกขนาดหน้าจอ
- ✅ **Animated Background** - พื้นหลังเคลื่อนไหว
- ✅ **Gradient Design** - ใช้สี gradient สอดคล้องกับแอป
- ✅ **Thai + English** - รองรับทั้งภาษาไทยและอังกฤษ
- ✅ **Contact Info** - แสดงอีเมลติดต่อ
- ✅ **Estimated Time** - แสดงเวลาที่คาดการณ์
- ✅ **Refresh Button** - ปุ่มลองใหม่อีกครั้ง
- ✅ **Brand Logo** - แสดงโลโก้ POLANODE

---

## 📱 Preview

หน้า Maintenance จะแสดง:
- โลโก้ POLANODE
- หัวข้อ "เว็บไซต์ปิดปรับปรุงชั่วคราว"
- ข้อความอธิบาย
- การ์ดแสดงเวลาที่คาดการณ์
- การ์ดแสดงอีเมลติดต่อ
- ปุ่ม "ลองใหม่อีกครั้ง"

---

## 🔧 Advanced: Custom Maintenance Screen

ถ้าต้องการสร้างหน้า maintenance แบบ custom:

1. สร้าง component ใหม่ใน `components/`
2. แก้ไข `App.tsx`:
   ```typescript
   if (MAINTENANCE_MODE) {
     return <YourCustomMaintenanceScreen />;
   }
   ```

---

## ⚠️ หมายเหตุ

- ⚠️ **Maintenance Mode จะบล็อกการเข้าถึงแอปทั้งหมด**
- ⚠️ **ตรวจสอบให้แน่ใจว่าเปิด/ปิดถูกต้อง**
- ⚠️ **ใน Production ควรแจ้งผู้ใช้ล่วงหน้า**

---

## 📞 Support

ถ้ามีปัญหาหรือคำถาม:
- Email: support@thailife-rewards.com
- หรือดูเอกสารเพิ่มเติมใน `README.md`

---

*อัพเดทล่าสุด: $(date)*

